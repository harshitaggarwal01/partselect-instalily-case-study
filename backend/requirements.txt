# Web framework
fastapi>=0.100.0
uvicorn[standard]>=0.20.0

# LLM — Anthropic Claude (Sonnet + Haiku)
anthropic>=0.40.0

# Data validation
pydantic>=2.0.0
pydantic-settings>=2.0.0
typing_extensions>=4.0.0

# HTTP client — Voyage AI embeddings, live part lookup, scraping
httpx>=0.24.0

# Environment file loading
python-dotenv>=1.0.0

# Vector similarity search
numpy>=1.24.0

# Python 3.8/3.9 compatibility for deferred type annotations with Pydantic v2
eval_type_backport>=0.1.3
